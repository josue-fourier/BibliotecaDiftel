server:
  cacheDir: /home/filebrowser/data/tmp # using the data volume so it can persist across restarts
  sources:
    - path: /srv/safe # Do not use a root "/" directory or include the "/var" folder
      config:
        defaultEnabled: true
    - path: /srv/recursos # Do not use a root "/" directory or include the "/var" folder
      config:
        defaultEnabled: true
