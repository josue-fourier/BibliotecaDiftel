# Database Settings
POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=default_database
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Django Settings (Recuerda cambiar en PROD)
SECRET_KEY="django-insecure-change-me-for-prod"
DEBUG=False
ALLOWED_HOSTS="127.0.0.1,localhost,tudominio.cl"
CSRF_TRUSTED_ORIGINS="http://127.0.0.1:8080,http://127.0.0.1:8000,https://tudominio.cl"
ADMIN_URL="/admin"

# Webhooks para Notificaciones N8N
N8N_WEBHOOK_CLEAN=""
N8N_WEBHOOK_THREAT=""

# Email Configuration (SMTP)
# Cambia a EMAIL_MAILER=newsletters para enviar correos reales por SMTP
EMAIL_MAILER=default
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
BULK_EMAIL_SERVICE_ACCOUNT_ID=diftel@usm.cl
BULK_EMAIL_SERVICE_API_KEY=app-password
EMAIL_USE_TLS=True

# Cloudflare Tunnel (Opcional)
CLOUDFLARE_TOKEN=""
